# บันทึกการเปลี่ยนแปลง

รูปแบบตาม [Keep a Changelog](https://keepachangelog.com/) · เลขเวอร์ชันตาม [Semantic Versioning](https://semver.org/lang/th/)

---

## [1.3.0] — 22 สิงหาคม 2569

รองรับโครงสร้างแบบสอบถามที่ปรับใหม่ก่อนรอบ tryout ซึ่งเปลี่ยน 5 จุดพร้อมกัน และเปิดใช้การตรวจการนอนหลับที่ออกแบบไว้ตั้งแต่ต้นแต่ยังใช้ไม่ได้เพราะขาดฟิลด์

### แก้ไข

**วันเกิดอ่านไม่ได้ทั้งไฟล์** — แบบสอบถามเปลี่ยนจากช่องวันที่ช่องเดียวเป็น dropdown สามช่อง (วัน / เดือนภาษาไทย / ปี พ.ศ.) และตอนส่งออก หัวคอลัมน์ของทั้งสามช่องออกมาเป็นค่าว่าง เหลือเพียงคอลัมน์หัวข้อ `วัน เดือน ปีเกิด (ตามบัตรประชาชน)` ที่ไม่มีข้อมูลอยู่ข้างใน ระบบจึงอ่านคอลัมน์เปล่าแล้วสรุปว่าไม่มีวันเกิด ทำให้สัญญาณ `I4` `I5` `I6` และการแตกชุดคำถามตามอายุใช้งานไม่ได้ทั้งหมด

ตอนนี้ระบบตรวจเองว่าหากคอลัมน์ถัดจาก `ปีเกิด` มีหัวคอลัมน์ว่างสามคอลัมน์ติดกัน จะอ่านเป็น วัน เดือน และปี แล้วประกอบเป็นวันเกิด พร้อมแปลง พ.ศ. เป็น ค.ศ. อัตโนมัติ และยังอ่านไฟล์รูปแบบเดิมที่เป็นคอลัมน์เดียวได้เหมือนเดิม

**ผู้สัมภาษณ์และโรงเรียนถูกแตกเป็นหลายคอลัมน์** — ชื่อผู้สัมภาษณ์แยกเป็น 4 คอลัมน์ตามทีม และชื่อโรงเรียนแยกเป็น 15 คอลัมน์ตามอำเภอ ระบบรวมค่าจากคอลัมน์ที่ไม่ว่างให้อัตโนมัติ

**มาตรวัดสุขภาวะในโรงเรียนหายทั้งหน้า** — หัวคอลัมน์ตัดข้อความนำออก เหลือเพียง `[ข้อความ]` ระบบจึงหาคอลัมน์ไม่พบ เพิ่มการตรวจหาแบบสำรองจากรูปแบบหัวคอลัมน์ที่ขึ้นต้นด้วยวงเล็บเหลี่ยม และเพิ่มตัวเลือก `เฉยๆ` ที่เปลี่ยนมาจาก `รู้สึกเฉยๆ` เข้าในตารางแปลงคะแนน

**คอลัมน์เวลาเริ่มทำแบบสอบถามชื่อ `วันเริ่มต้น`** ซึ่งเดิมตรวจหาเฉพาะ `startdate` และคำขึ้นต้นภาษาไทยแบบอื่น

### เพิ่ม

**การตรวจการนอนหลับ** — ส่วน J01 และ J02 มีอยู่ในไฟล์ส่งออกแล้ว จึงเปิดใช้สัญญาณที่ออกแบบไว้ได้จริง

- `S6` ระยะเวลานอนผิดปกติ (นอก 4–14 ชั่วโมง) · `S7` เวลาเข้านอนผิดปกติ · `S8` เวลาตื่นนอนผิดปกติ · `S9` เวลาเข้านอนกับตื่นนอนเป็นเวลาเดียวกัน
- เวลานอนถูกนำไปรวมในภาพรวมเวลาทั้งวัน ทำให้เกณฑ์ 1,440 นาทีมีความหมายขึ้นมาก เพราะการนอนเป็นก้อนเวลาที่ใหญ่ที่สุดของวัน แผงภาพรวมเวลาและรายละเอียดเคสสลับข้อความเองตามว่าไฟล์มีข้อมูลการนอนหรือไม่

**`I11` วันเกิดกรอกไม่ครบสามช่อง** — จับกรณีกรอกมาบางช่อง หรือกรอกเป็นวันที่ที่ไม่มีอยู่จริง เช่น 31 เมษายน ซึ่งเป็นความเสี่ยงเฉพาะของรูปแบบ dropdown สามช่อง

**ตัวอ่านเดือนภาษาไทย** — รองรับชื่อเต็ม คำย่อแบบ `ก.ย.` และตัวสะกดที่คลาดเคลื่อน โดยเฉพาะ `พฤษจิกายน` ซึ่งเป็นตัวสะกดที่ปรากฏใน dropdown ของระบบปัจจุบัน (ที่ถูกคือ `พฤศจิกายน` ควรแก้ที่ต้นทาง)

**เกณฑ์ปรับค่าได้ 2 ตัว** (รวมเป็น 28) — `sleepMinH` `sleepMaxH`

**แสดง `ห้อง` และ `ระบุทีม`** ในรายละเอียดเคสหน้าข้อมูลทั่วไป

### เปลี่ยนแปลง

- รวมสัญญาณระดับเคสเป็น 79 รายการ จากเดิม 74
- รายการฟิลด์ที่ยังตรวจไม่ได้ในหน้าอัปโหลด ตัดส่วนการนอนหลับและห้องเรียนออก เหลือความยินยอม รหัสประจำตัววิจัย รหัสโรงเรียน ทะเบียนเป้าหมาย และสถานะเคส

---

## [1.2.0] — 20 สิงหาคม 2569

ปรับโครงสร้างการแสดงผลตามมติที่ประชุมทีมผู้ใช้งาน แยกหน้าตรวจรายเคสให้ตรงกับส่วนของแบบสอบถาม และเพิ่มการแสดงผลข้อมูลจริงของทุกส่วน

### เปลี่ยนชื่อ

| เดิม | เมนู | หัวข้อหน้า |
|---|---|---|
| ตรวจระดับชุดข้อมูล | ภาพรวมชุดข้อมูล | ภาพรวมความสำคัญและความสอดคล้องของชุดข้อมูล |
| ทะเบียน · อายุ | ข้อมูลทั่วไป | ข้อมูลส่วนบุคคลและข้อมูลทั่วไป |
| เช็คก่อนออกจากโรงเรียน | QuickCheck | QuickCheck |
| มาตรวัดสิ่งแวดล้อม | สุขภาวะในโรงเรียน | สุขภาวะในโรงเรียน |

คำว่า “งบเวลา” เปลี่ยนเป็น “ภาพรวมเวลา” ทั้งระบบ ทั้งชื่อสัญญาณ คำอธิบายในอภิธานศัพท์ ป้ายในรายละเอียดเคส ชื่อเกณฑ์ในหน้าตั้งค่า และหัวแผงแจกแจง

### เพิ่ม

**แยกหน้าตรวจรายเคสจาก 5 เป็น 8 หน้า** — ข้อมูลทั่วไป · PA เวลาเรียน (จันทร์–ศุกร์) · PA วันหยุด (เสาร์–อาทิตย์) · เรียนพิเศษ · หน้าจอ · ความพึงพอใจ · สุขภาวะในโรงเรียน · การบริโภคโซเดียม

**กลไกจำกัดขอบเขตระดับข้อคำถาม** — สัญญาณที่ผูกกับข้อใดข้อหนึ่งถูกติดหมายเลขข้อไว้ (`f.item`) หน้าจึงกรองได้ทั้งด้วยรหัสสัญญาณและด้วยหมายเลขข้อ ผ่านฟังก์ชัน `flagsOn(rec, page)` ตัวเลขบนเมนู การ์ดสัญญาณ แผงแจกแจง ตารางเคส และรายละเอียดเคส ใช้กลไกเดียวกันทั้งหมด

ข้อ 15 และ 16 ถูกกำหนดเป็น `SHARED_ITEMS` จึงปรากฏทั้งในหน้า PA เวลาเรียนและ PA วันหยุด ตามที่แบบสอบถามระบุว่าถามวันเสาร์อาทิตย์ด้วย พร้อมป้าย “คาบ” กำกับ

**หน้าความคืบหน้าการเก็บข้อมูล** — ตารางไขว้โรงเรียนแนวตั้ง วันที่แนวนอนเรียงใหม่ไปเก่า ตรึงชื่อโรงเรียนไว้ซ้าย คอลัมน์รวมไว้ขวา แถวรวมรายวันไว้ล่าง และหัวตารางไว้บน พร้อมตัวเลขภาพรวม 4 ใบ

**รายงานสรุป PDF** — ใช้ระบบพิมพ์ของเบราว์เซอร์กับ print stylesheet เฉพาะ เพื่อให้ภาษาไทยถูกต้องและคัดลอกข้อความได้ ไม่ต้องเพิ่มไลบรารี รายงานมี 5 ส่วน คือ ภาพรวม · สรุปรายโรงเรียน · สรุปรายผู้สัมภาษณ์ · สัญญาณระดับชุดข้อมูล · สัญญาณที่พบเรียงตามจำนวน ใช้ตัวกรองที่ตั้งไว้บนหน้าจอ และระบุขอบเขตที่กรองไว้ในหัวรายงาน

**ตัวกรองสัมพันธ์กัน** — เลือกโรงเรียนแล้วช่องผู้สัมภาษณ์เหลือเฉพาะคนที่เก็บโรงนั้น และในทางกลับกัน ทุกตัวเลือกแสดงจำนวนเคสในวงเล็บ ถ้าเลือกคู่ที่เข้ากันไม่ได้ ระบบรีเซ็ตตัวกรองผู้สัมภาษณ์พร้อมแสดงเหตุผล และสลับให้อัตโนมัติเมื่อวันที่เริ่มต้นอยู่หลังวันที่สิ้นสุด เพิ่มปุ่มล้างตัวกรอง

**รองรับคอลัมน์ระยะเวลาสัมภาษณ์** — ตรวจหา `interviewtime` และ `startdate` อัตโนมัติ พบก็เปิดสัญญาณ `T1` `T2` `T3` เอง ไม่พบก็ถอยไปใช้ระยะห่างระหว่างเคสเหมือนเดิม ไฟล์เดียวรองรับทั้งสองแบบ

**สัญญาณระดับเคส 3 รายการ** (รวมเป็น 74)

- `T1` สัมภาษณ์เร็วผิดปกติ · `T2` สัมภาษณ์เร็วกว่าปกติ — เกณฑ์คิดเป็นเปอร์เซ็นต์ของมัธยฐานทีมในไฟล์นั้น ไม่ใช่ตัวเลขนาทีตายตัว จึงปรับตัวตามข้อมูลจริงเอง
- `T3` ระยะเวลาสัมภาษณ์ยาวผิดปกติ

**เกณฑ์ปรับค่าได้ 3 ตัว** (รวมเป็น 26) — `durCritPct` `durWarnPct` `durLongMin`

**แผงแจกแจงของทุกหน้า** — ข้อมูลทั่วไปแสดงการกระจายระดับชั้น เพศ และอายุ · PA ทั้งสองหน้าแสดงตารางสรุปรายข้อและตารางระดับความเหนื่อยพร้อมคอลัมน์ที่ติ๊กครบสามระดับ · เรียนพิเศษแสดงการกระจายจำนวนวันและชั่วโมง · หน้าจอแสดงฮิสโตแกรมและองค์ประกอบของภาพรวมเวลา · ความพึงพอใจแสดง Reach รายกิจกรรม · สุขภาวะแสดงค่าเฉลี่ยรายข้อและเปรียบเทียบสองกลุ่มย่อย (ข้อ 1–6 กับ 7–9)

### เปลี่ยนแปลง

- `D3` `D4` ย้ายไปหน้า PA เวลาเรียน เพราะคำนวณจากข้อ 1–16 อย่างเดียว ไม่ได้ใช้เรียนพิเศษหรือหน้าจอ
- `D1` `D2` อยู่ท้ายหน้าหน้าจอในบล็อก “ภาพรวมเวลารวมทั้งวัน” เพราะเป็นหน้าสุดท้ายของสามส่วนที่นำมารวมกัน และสาเหตุที่ทำให้เวลารวมผิดปกติมักมาจากช่องหน้าจอ
- `T7` เปลี่ยนตรรกะเมื่อไฟล์มีคอลัมน์เวลา จากเดิมดูแค่ว่าเคสห่างกันน้อยกว่า 10 นาที เป็นดูว่าห่างกันน้อยกว่าระยะเวลาสัมภาษณ์ที่ระบุไว้หรือไม่ ซึ่งเป็นความขัดแย้งที่ชัดเจนกว่า
- หน้าผู้สัมภาษณ์และรายงาน PDF สลับคอลัมน์ระหว่าง “ระยะเวลาสัมภาษณ์” กับ “ห่างเฉลี่ย” ตามว่าไฟล์มีคอลัมน์เวลาหรือไม่
- ย่อระยะห่างและขนาดตัวอักษรของปุ่มเมนู ให้เมนู 15 รายการพอดีจอโน้ตบุ๊ก
- รายละเอียดเคสที่กางออกมาแสดงเฉพาะข้อมูลของหน้านั้น จากเดิมแสดงทุกส่วนรวมกันเป็นก้อนเดียว

### แก้ไข

- การเดาหน่วยของคอลัมน์ `interviewtime` จากขนาดตัวเลขทำให้เคสที่สัมภาษณ์เร็ว (ต่ำกว่า 1,000 วินาที) ถูกอ่านเป็นนาที แล้วกลายเป็นเคสที่ใช้เวลานานผิดปกติแทน เปลี่ยนไปดูจากข้อความหัวคอลัมน์แทน โดยถือเป็นวินาทีตามค่าเริ่มต้นของ LimeSurvey เว้นแต่หัวคอลัมน์ระบุว่าเป็นนาที

---

## [1.1.0] — 17 สิงหาคม 2569

เพิ่มการตรวจและการแสดงผลสำหรับส่วนที่เดิมมีแต่การตรวจ routing ยังไม่มีการตรวจเนื้อหาและไม่แสดงข้อมูลจริง

### เพิ่ม

**เงื่อนไขระดับเคส 19 รายการ** (รวมเป็น 71 รายการ)

- `S12` `S13` `S14` `S15` `S16` `S17` `S18` — เรียนพิเศษและพฤติกรรมหน้าจอ (ข้อ 18–19) ครอบคลุมการไม่ตอบคำถามกรอง การกรอกรายละเอียดไม่ครบ การกรอกนาทีลงช่องชั่วโมง และรูปแบบเวลาหน้าจอที่ขัดกับพฤติกรรมปกติ
- `X7` `X8` `X9` — ความพึงพอใจ (ข้อ 20) รวมถึงการตรวจข้ามเคสภายในโรงเรียนเดียวกัน เมื่อมีนักเรียนอ้างว่าโรงเรียนไม่ได้จัดกิจกรรมขณะที่เพื่อนตอบว่าเคยเข้าร่วม
- `Q10` `Q11` — มาตรวัดสิ่งแวดล้อมโรงเรียน 9 ข้อ
- `N1`–`N7` — การบริโภคเกลือและโซเดียม ตรวจความครบถ้วนของชุดและความสอดคล้องระหว่างข้อภายในชุดเดียวกัน

**เงื่อนไขระดับชุดข้อมูล 3 รายการ** (รวมเป็น 8 รายการ)

- `B6` ข้อในมาตรวัดสิ่งแวดล้อมที่ไม่มีความแปรปรวน
- `B7` คำถามความรู้เรื่องโซเดียมที่มีคำตอบซ้ำกันเกือบทั้งชุด
- `B8` สัดส่วนการเข้าร่วมกิจกรรมต่ำผิดปกติ

**แผงแจกแจงคำตอบ** — แต่ละหน้าแสดงการกระจายคำตอบจริงรายข้อ ไม่ใช่เฉพาะรายการสัญญาณ ได้แก่ การกระจายเรียนพิเศษและเวลาหน้าจอ ตารางการเข้าร่วมและคะแนนความพึงพอใจ ค่าเฉลี่ยรายข้อของมาตรวัดสิ่งแวดล้อม และการกระจายคำตอบรายข้อของชุดโซเดียมทั้งสองช่วงอายุ

**หน้าใหม่** — การบริโภคโซเดียม

**เกณฑ์ปรับค่าได้ 5 ตัว** (รวมเป็น 23 ตัว) — `tutorWdHrMax` `tutorWeHrMax` `envLowMean` `reachLowPct` `naSamePct`

### เปลี่ยนแปลง

- หน้า "งบเวลา · หน้าจอ" เปลี่ยนชื่อเป็น "เรียนพิเศษ · พฤติกรรมเนือยนิ่ง · หน้าจอ"
- หน้า "ความสอดคล้อง" แยกเป็น "ความพึงพอใจ · สิ่งแวดล้อมโรงเรียน" และ "การบริโภคโซเดียม"
- รายละเอียดเคส (กดที่แถวในตาราง) แสดงข้อ 18–20 มาตรวัดสิ่งแวดล้อม และชุดโซเดียมของเคสนั้นครบทุกข้อ

### แก้ไข

- `S18` และ `D1` `D2` ไม่รายงานซ้ำเมื่อสาเหตุที่แท้จริงคือช่องหน้าจอกรอกผิดหน่วย (`S2` หรือ `S15`) เพื่อลดสัญญาณซ้ำซ้อนที่ชี้ไปที่ความผิดพลาดเดียวกัน
- ป้ายกำกับข้อ 20 ไม่แสดงเลขข้อซ้ำสองครั้ง

### เอาออก

- `Q9` (อัตราข้อมูลขาดหายรายเคส) — คำนวณให้ถูกต้องไม่ได้หากไม่มี codebook ระบุว่าข้อใดบ้างที่แต่ละเคสควรตอบ ย้ายไปอยู่ในรายการเงื่อนไขที่รอฟิลด์เพิ่มเติม

---

## [1.0.0] — 17 สิงหาคม 2569

รุ่นแรก พัฒนาต่อยอดสถาปัตยกรรมจาก SPA14 Monitor โดยออกแบบเงื่อนไขใหม่ทั้งหมดให้ตรงกับโครงสร้างแบบสอบถามเชิงลึกของโครงการ

### เพิ่ม

- อ่านไฟล์ส่งออกจาก LimeSurvey ทั้ง `.csv` และ `.xlsx` โดยจับคู่คอลัมน์อัตโนมัติจากรูปแบบหัวคอลัมน์ที่เป็นข้อความคำถาม
- เงื่อนไขระดับเคส 52 รายการ ครอบคลุมหมวด `I` ทะเบียนและอายุ · `P` กิจกรรมทางกาย 17 ช่วงเวลา · `D` งบเวลา · `S` เนือยนิ่งและหน้าจอ · `X` ความพึงพอใจ · `Q` รูปแบบการตอบและ routing · `T` จังหวะการเก็บ
- เงื่อนไขระดับชุดข้อมูล 5 รายการ (`B1`–`B5`)
- เงื่อนไขระดับผู้สัมภาษณ์ 4 รายการ (`Z2` `Z3` `Z4` `Z6`) เปรียบเทียบกับค่าเฉลี่ยทีม
- ระบบหน้าต่างการแก้ไข แบ่งเป็น แก้ที่โรงเรียน · ตามกลับในสัปดาห์ · แก้ที่สำนักงาน
- หน้า "เช็คก่อนออกจากโรงเรียน" กรองเฉพาะรายการที่ต้องปิดก่อนทีมออกจากพื้นที่ จัดกลุ่มตามโรงเรียนและวันที่
- หน้า "ตรวจระดับชุดข้อมูล" พร้อมตารางสรุปรายข้อกิจกรรมทางกาย 17 ข้อ สำหรับใช้ปรับเกณฑ์หลังทดสอบเครื่องมือ
- หน้า "เกณฑ์การตรวจ" ปรับเพดานเวลารายข้อและเกณฑ์เชิงตัวเลขได้โดยไม่ต้องแก้โปรแกรม
- ส่งออกรายการที่ต้องตรวจเป็น CSV
- อภิธานศัพท์อธิบายทุกสัญญาณ พร้อมคำแนะนำการดำเนินการ

### หมายเหตุ

เกณฑ์เชิงตัวเลขทั้งหมดเป็นค่าตั้งต้นที่ประเมินจากโครงสร้างเวลาเรียนของโรงเรียนไทยโดยทั่วไป **ต้องปรับให้ตรงกับบริบทพื้นที่หลังการทดสอบเครื่องมือ (pre-test)** ก่อนใช้เก็บข้อมูลจริง
