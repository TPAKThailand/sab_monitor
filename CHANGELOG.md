# บันทึกการเปลี่ยนแปลง

รูปแบบตาม [Keep a Changelog](https://keepachangelog.com/) · เลขเวอร์ชันตาม [Semantic Versioning](https://semver.org/lang/th/)

---

## [1.1.0] — 17 สิงหาคม 2569

เพิ่มการตรวจและการแสดงผลสำหรับส่วนที่เดิมมีแต่การตรวจ routing ยังไม่มีการตรวจเนื้อหาและไม่แสดงข้อมูลจริง

### เพิ่ม

**เงื่อนไขระดับเคส 19 รายการ** (รวมเป็น 71 รายการ)

- `S12` `S13` `S14` `S15` `S16` `S17` `S18` — เรียนพิเศษและพฤติกรรมหน้าจอ (ข้อ 18–19) ครอบคลุมการไม่ตอบคำถามกรอง การกรอกรายละเอียดไม่ครบ การกรอกนาทีลงช่องชั่วโมง และรูปแบบเวลาหน้าจอที่ขัดกับพฤติกรรมปกติ
- `X7` `X8` `X9` — ความพึงพอใจ (ข้อ 20) รวมถึงการตรวจข้ามเคสภายในโรงเรียนเดียวกัน เมื่อมีนักเรียนอ้างว่าโรงเรียนไม่ได้จัดกิจกรรมขณะที่เพื่อนตอบว่าเคยเข้าร่วม
- `Q10` `Q11` — มาตรวัดสิ่งแวดล้อมโรงเรียน 9 ข้อ
- `N1`–`N7` — การบริโภคเกลือและโซเดียม ตรวจความครบถ้วนของชุดและความสอดคล้องระหว่างข้อภายในชุดเดียวกัน

**เงื่อนไขระดับชุดข้อมูล 3 รายการ** (รวมเป็น 8 รายการ)

- `B6` ข้อในมาตรวัดสิ่งแวดล้อมที่ไม่มีความแปรปรวน
- `B7` คำถามความรู้เรื่องโซเดียมที่มีคำตอบซ้ำกันเกือบทั้งชุด
- `B8` สัดส่วนการเข้าร่วมกิจกรรมต่ำผิดปกติ

**แผงแจกแจงคำตอบ** — แต่ละหน้าแสดงการกระจายคำตอบจริงรายข้อ ไม่ใช่เฉพาะรายการสัญญาณ ได้แก่ การกระจายเรียนพิเศษและเวลาหน้าจอ ตารางการเข้าร่วมและคะแนนความพึงพอใจ ค่าเฉลี่ยรายข้อของมาตรวัดสิ่งแวดล้อม และการกระจายคำตอบรายข้อของชุดโซเดียมทั้งสองช่วงอายุ

**หน้าใหม่** — การบริโภคโซเดียม

**เกณฑ์ปรับค่าได้ 5 ตัว** (รวมเป็น 23 ตัว) — `tutorWdHrMax` `tutorWeHrMax` `envLowMean` `reachLowPct` `naSamePct`

### เปลี่ยนแปลง

- หน้า "งบเวลา · หน้าจอ" เปลี่ยนชื่อเป็น "เรียนพิเศษ · พฤติกรรมเนือยนิ่ง · หน้าจอ"
- หน้า "ความสอดคล้อง" แยกเป็น "ความพึงพอใจ · สิ่งแวดล้อมโรงเรียน" และ "การบริโภคโซเดียม"
- รายละเอียดเคส (กดที่แถวในตาราง) แสดงข้อ 18–20 มาตรวัดสิ่งแวดล้อม และชุดโซเดียมของเคสนั้นครบทุกข้อ

### แก้ไข

- `S18` และ `D1` `D2` ไม่รายงานซ้ำเมื่อสาเหตุที่แท้จริงคือช่องหน้าจอกรอกผิดหน่วย (`S2` หรือ `S15`) เพื่อลดสัญญาณซ้ำซ้อนที่ชี้ไปที่ความผิดพลาดเดียวกัน
- ป้ายกำกับข้อ 20 ไม่แสดงเลขข้อซ้ำสองครั้ง

### เอาออก

- `Q9` (อัตราข้อมูลขาดหายรายเคส) — คำนวณให้ถูกต้องไม่ได้หากไม่มี codebook ระบุว่าข้อใดบ้างที่แต่ละเคสควรตอบ ย้ายไปอยู่ในรายการเงื่อนไขที่รอฟิลด์เพิ่มเติม

---

## [1.0.0] — 17 สิงหาคม 2569

รุ่นแรก พัฒนาต่อยอดสถาปัตยกรรมจาก SPA14 Monitor โดยออกแบบเงื่อนไขใหม่ทั้งหมดให้ตรงกับโครงสร้างแบบสอบถามเชิงลึกของโครงการ

### เพิ่ม

- อ่านไฟล์ส่งออกจาก LimeSurvey ทั้ง `.csv` และ `.xlsx` โดยจับคู่คอลัมน์อัตโนมัติจากรูปแบบหัวคอลัมน์ที่เป็นข้อความคำถาม
- เงื่อนไขระดับเคส 52 รายการ ครอบคลุมหมวด `I` ทะเบียนและอายุ · `P` กิจกรรมทางกาย 17 ช่วงเวลา · `D` งบเวลา · `S` เนือยนิ่งและหน้าจอ · `X` ความพึงพอใจ · `Q` รูปแบบการตอบและ routing · `T` จังหวะการเก็บ
- เงื่อนไขระดับชุดข้อมูล 5 รายการ (`B1`–`B5`)
- เงื่อนไขระดับผู้สัมภาษณ์ 4 รายการ (`Z2` `Z3` `Z4` `Z6`) เปรียบเทียบกับค่าเฉลี่ยทีม
- ระบบหน้าต่างการแก้ไข แบ่งเป็น แก้ที่โรงเรียน · ตามกลับในสัปดาห์ · แก้ที่สำนักงาน
- หน้า "เช็คก่อนออกจากโรงเรียน" กรองเฉพาะรายการที่ต้องปิดก่อนทีมออกจากพื้นที่ จัดกลุ่มตามโรงเรียนและวันที่
- หน้า "ตรวจระดับชุดข้อมูล" พร้อมตารางสรุปรายข้อกิจกรรมทางกาย 17 ข้อ สำหรับใช้ปรับเกณฑ์หลังทดสอบเครื่องมือ
- หน้า "เกณฑ์การตรวจ" ปรับเพดานเวลารายข้อและเกณฑ์เชิงตัวเลขได้โดยไม่ต้องแก้โปรแกรม
- ส่งออกรายการที่ต้องตรวจเป็น CSV
- อภิธานศัพท์อธิบายทุกสัญญาณ พร้อมคำแนะนำการดำเนินการ

### หมายเหตุ

เกณฑ์เชิงตัวเลขทั้งหมดเป็นค่าตั้งต้นที่ประเมินจากโครงสร้างเวลาเรียนของโรงเรียนไทยโดยทั่วไป **ต้องปรับให้ตรงกับบริบทพื้นที่หลังการทดสอบเครื่องมือ (pre-test)** ก่อนใช้เก็บข้อมูลจริง
