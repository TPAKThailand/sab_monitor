# คู่มือการดูแลและแก้ไขระบบ

เอกสารนี้สำหรับผู้ที่ต้องแก้ไขหรือดูแลระบบต่อ ไม่จำเป็นต้องอ่านถ้าเป็นผู้ใช้งานทั่วไป

---

## โครงสร้างไฟล์

ทั้งระบบอยู่ใน `index.html` ไฟล์เดียว แบ่งเป็นสามส่วนตามลำดับ

| ส่วน | เนื้อหา |
|---|---|
| `<style>` | ระบบดีไซน์ ตารางไขว้ และ print stylesheet สำหรับรายงาน PDF |
| `<script>` ก้อนแรก | ไลบรารี SheetJS สำหรับอ่าน `.xlsx` — **ไม่ต้องแก้** |
| `<script>` ก้อนที่สอง | ตรรกะของระบบทั้งหมด |

ก้อนที่สองมีหมายเลขกำกับในคอมเมนต์

1. เกณฑ์การตรวจ — `PA_ITEMS` และ `MISC`
2. ทะเบียนสัญญาณ — `FLAGS` และ `BATCH_FLAGS`
3. ยูทิลิตี้ — รวมตัวอ่าน CSV ที่เขียนเอง
4. จับคู่คอลัมน์ — `buildMap()`
5. แปลงข้อมูลและตรวจสัญญาณ — `buildRows()` `evaluate()` `staffStats()` `batchFlags()`
6. UI — 6a การจำกัดขอบเขตของหน้า · 6b แผงแจกแจง · 6c รายงาน PDF
7. เรนเดอร์และเหตุการณ์ — `render()` `gotoPage()` `syncFilters()`

ไม่มีขั้นตอน build ไม่มี dependency แก้ไฟล์แล้วเปิดในเบราว์เซอร์ได้ทันที

---

## แนวคิดสำคัญ การจำกัดขอบเขตของหน้า

หน้าตรวจรายเคสทั้งแปดหน้าใช้ตรรกะเดียวกัน ต่างกันแค่ว่ารับผิดชอบสัญญาณและข้อคำถามใดบ้าง กำหนดไว้ในส่วนที่ 6a

```js
PAGE_CODES = { paw:['P1','P2',...], pah:[...], tutor:[...], ... }   // สัญญาณของหน้านั้น
PAGE_ITEMS = { paw:new Set([1..16]), pah:new Set([15,16,17]), ... }  // ข้อคำถามของหน้านั้น
SHARED_ITEMS = new Set([15,16])                                      // ข้อที่นับสองหน้า
```

`flagsOn(rec, page)` คือหัวใจ ทำงานสองชั้น

1. กรองด้วย `PAGE_CODES` ว่ารหัสสัญญาณอยู่ในหน้านี้หรือไม่
2. ถ้าสัญญาณนั้นผูกกับข้อคำถาม (`f.item` ไม่เป็น `null`) กรองซ้ำด้วย `PAGE_ITEMS`

สัญญาณที่ไม่ผูกกับข้อใดข้อหนึ่ง เช่น `P8` `P9` `P10` `P11` `D3` `D4` มี `item` เป็น `null` จึงผ่านชั้นที่สองเสมอ และไปปรากฏในหน้าที่มีรหัสนั้นอยู่ใน `PAGE_CODES`

ฟังก์ชันนี้ถูกใช้ทั้งในการนับตัวเลขบนเมนู การ์ดสัญญาณ ตารางเคส และรายละเอียดเคส **ถ้าจะเพิ่มที่แสดงผลใหม่ ต้องเรียกผ่าน `flagsOn` เสมอ** ไม่ใช่อ่าน `rec.flags` ตรง ๆ ไม่งั้นหน้านั้นจะแสดงสัญญาณของส่วนอื่นปนมา

---

## งานที่ต้องทำบ่อยที่สุด

### ปรับเกณฑ์เชิงตัวเลข

ปรับชั่วคราวได้ในหน้า **เกณฑ์การตรวจ** แต่ค่าจะหายเมื่อปิดหน้าเว็บ ถ้าได้ค่าที่ลงตัวแล้วให้แก้ค่าเริ่มต้นในโค้ด

- เพดานเวลาและจำนวนวันรายข้อ → `PA_ITEMS` (ส่วนที่ 1)
- เกณฑ์อื่นทั้งหมด → `MISC` (ส่วนที่ 1)

### เพิ่มสัญญาณใหม่

ต้องแก้สามจุด ถ้าลืมจุดใดจุดหนึ่งสัญญาณจะไม่ปรากฏบนหน้าจอ

**หนึ่ง** เพิ่มรายการใน `FLAGS` (ส่วนที่ 2)

```js
S20:{page:'screen', sev:'warn', fix:'onsite', name:'ชื่อสั้น ๆ ของสัญญาณ',
    mean:'อธิบายว่าสัญญาณนี้หมายความว่าอะไร และทำไมจึงน่าสงสัย',
    act:'บอกว่าทีมสนามต้องทำอะไรต่อ ให้เป็นคำสั่งที่ทำตามได้จริง'},
```

- `page` — `reg` `paw` `pah` `tutor` `screen` `sat` `well` `na` `staff`
- `sev` — `crit` ตรวจด่วน · `warn` ควรตรวจสอบ · `watch` เฝ้าระวัง
- `fix` — `onsite` แก้ที่โรงเรียน · `sameweek` ตามกลับในสัปดาห์ · `office` แก้ที่สำนักงาน

**สอง** เพิ่มตรรกะใน `evaluate()` (ส่วนที่ 5)

```js
if (เงื่อนไข) F(r, 'S20', `รายละเอียดที่เห็นในตาราง เช่น ค่าที่ผิด ${fmt(ค่า)}`, itemNo);
```

พารามิเตอร์ที่สี่คือหมายเลขข้อคำถาม **ต้องใส่ถ้าสัญญาณผูกกับข้อใดข้อหนึ่ง** ไม่งั้นการแยกหน้า PA เวลาเรียนกับ PA วันหยุดจะไม่ทำงาน ถ้าเป็นสัญญาณที่ดูข้ามหลายข้อให้ละไว้

ข้อความ detail ควรบอก **ค่าที่พบจริง** ไม่ใช่แค่บอกว่าผิด เพราะทีมสนามต้องใช้ตัดสินใจว่าจะแก้อย่างไร

**สาม** เพิ่มรหัสลงใน `PAGE_CODES` ของหน้านั้น (ส่วนที่ 6a) ลำดับในอาร์เรย์คือลำดับที่การ์ดจะเรียงบนหน้าจอ

### เพิ่มหน้าใหม่

1. เพิ่มปุ่มใน `<nav>` พร้อม `data-page` และ `<span class="cnt" id="cnt-ชื่อหน้า">`
2. เพิ่ม `<section class="page" id="page-ชื่อหน้า">` ที่มี `ชื่อหน้าCards` `ชื่อหน้าExplain` `ชื่อหน้าPanels` `ชื่อหน้าCount` `ชื่อหน้าTable`
3. เพิ่ม key ใน `PAGE_CODES` และ `S.sel`
4. เพิ่มชื่อหน้าใน `CASE_PAGES` และ `ID` ใน `render()` (ส่วนที่ 7)
5. เขียนฟังก์ชันแผงแล้วผูกใน `PANEL`
6. เพิ่ม branch ใน `caseDetail()` ให้แสดงเฉพาะข้อมูลของหน้านั้น

### แก้เมื่อไฟล์ส่งออกเปลี่ยนรูปแบบ

`buildMap()` (ส่วนที่ 4) จับคู่คอลัมน์จากข้อความในหัวคอลัมน์ ถ้าแก้ข้อความคำถามใน LimeSurvey หรือสลับลำดับข้อ ระบบอาจจับคู่ผิด

จุดที่เปราะที่สุดคือบล็อกกิจกรรมทางกาย ซึ่งอาศัยว่าหัวคอลัมน์ขึ้นต้นด้วย `"เลข. "` และคอลัมน์ถัดไปมีข้อความ `"สัปดาห์ละกี่วัน"` แล้วนับ 5 คอลัมน์ถัดไปตามลำดับ วัน · นาที · ระดับเหนื่อย 3 ระดับ

ถ้าจับคู่ผิด อาการที่เห็นคือหน้ากิจกรรมทางกายว่างเปล่า หรือค่านาทีไปโผล่ผิดข้อ วิธีตรวจเร็วที่สุดคือเปิด Console แล้วดู `S.map`

**บทเรียนจากรุ่น tryout** การเปลี่ยนแบบสอบถามครั้งเดียวทำให้ไฟล์ส่งออกเปลี่ยนพร้อมกันถึงห้าจุด และมีเพียงจุดเดียวที่มีอาการชัดเจน อีกสี่จุดเงียบ ๆ ทำให้บางหน้าว่างเปล่าโดยไม่มี error รูปแบบความเสียหายที่เจอมีสี่แบบ ควรตรวจทั้งสี่แบบทุกครั้งที่ได้ไฟล์รุ่นใหม่

- **หัวคอลัมน์ว่าง** คำถามแบบ dropdown หลายช่องต่อกัน (เช่น วัน เดือน ปี) ส่งออกมาโดยมีหัวคอลัมน์เฉพาะช่องแรก ที่เหลือเป็นค่าว่าง ต้องหาโดยอ้างอิงตำแหน่งจากคอลัมน์หัวข้อ
- **คอลัมน์เดียวแตกเป็นหลายคอลัมน์** คำถามที่มีเงื่อนไขแสดงตามคำตอบก่อนหน้า เช่น ชื่อผู้สัมภาษณ์แยกตามทีม หรือชื่อโรงเรียนแยกตามอำเภอ ต้องรวมค่าจากคอลัมน์ที่ไม่ว่าง
- **หัวคอลัมน์ตัดข้อความนำออก** มาตรวัดที่เดิมมีข้อความนำร่วมกันทุกข้อ อาจเหลือเพียง `[ข้อความ]` ทำให้ตัวค้นหาเดิมหาไม่พบ ควรมีตัวตรวจหาสำรองเสมอ
- **ข้อความตัวเลือกเปลี่ยน** เช่น `รู้สึกเฉยๆ` เปลี่ยนเป็น `เฉยๆ` ทำให้ตารางแปลงคะแนนคืนค่า `null` เงียบ ๆ และค่าเฉลี่ยกลายเป็นขีด

วิธีตรวจที่เร็วที่สุดคือโหลดไฟล์ใหม่แล้วไล่ดูทุกหน้า หน้าที่ว่างเปล่าหรือมีขีดเต็มไปหมด คือหน้าที่การจับคู่พัง ไม่ใช่หน้าที่ข้อมูลสะอาด

### วันเกิดและเดือนภาษาไทย

`dobFromParts()` ประกอบวันเกิดจากสามช่อง แปลง พ.ศ. เป็น ค.ศ. เมื่อปีมากกว่า 2400 และคืนค่า `null` เมื่อได้วันที่ที่ไม่มีอยู่จริง เช่น 31 เมษายน ซึ่งจะไปขึ้นเป็นสัญญาณ `I11`

`TH_MONTH` รองรับชื่อเต็ม คำย่อ และตัวสะกดที่คลาดเคลื่อน การจับคู่ไล่จากคำยาวไปคำสั้นเสมอ มิฉะนั้น `มค` จะไปชนกับ `มกรา` ปัจจุบัน dropdown ในระบบสะกด `พฤษจิกายน` ซึ่งผิด ที่ถูกคือ `พฤศจิกายน` โค้ดรองรับทั้งสองแบบไว้ แต่ควรแก้ที่ต้นทาง

### คอลัมน์เวลาสัมภาษณ์

`buildMap()` ตรวจหา `startdate` และ `interviewtime` เอง และเดาหน่วยจากข้อความหัวคอลัมน์ — ถ้ามีคำว่า `นาที` หรือ `min` จะถือเป็นนาที ไม่งั้นถือเป็นวินาทีตามค่าเริ่มต้นของ LimeSurvey

**อย่าเดาหน่วยจากขนาดตัวเลข** เคยทำแบบนั้นแล้วพัง เพราะเคสที่สัมภาษณ์เร็ว (ต่ำกว่า 1000 วินาที) ถูกอ่านเป็นนาที กลายเป็นเคสที่ใช้เวลานานผิดปกติแทน

`S.hasDur` เป็น `true` เมื่อมีค่าเวลาอย่างน้อย 5 เคส หน้าผู้สัมภาษณ์และรายงาน PDF สลับการแสดงผลตามตัวแปรนี้

---

## การทดสอบ

ไม่มีชุดทดสอบอัตโนมัติในรีโพ วิธีตรวจว่าการแก้ไขไม่ทำให้อะไรพัง

1. เปิด `index.html` แล้วโหลดไฟล์ส่งออกจริง
2. กดเข้าทุกหน้าตามลำดับ ดูว่าไม่มีหน้าไหนว่างหรือค้าง
3. เปิด Console ดูว่าไม่มี error สีแดง
4. กดที่แถวในตารางเพื่อขยายรายละเอียดเคส ดูว่าแสดงเฉพาะข้อมูลของหน้านั้น และค่าตรงกับไฟล์ต้นทาง
5. ทดสอบตัวกรอง เลือกโรงเรียนแล้วดูว่าช่องผู้สัมภาษณ์เหลือเฉพาะคนที่เกี่ยวข้อง
6. ทดสอบกับไฟล์ส่งออกรุ่นเก่าอย่างน้อยหนึ่งไฟล์ เพื่อให้แน่ใจว่ายังอ่านย้อนหลังได้
7. กดปุ่มรายงาน PDF ดูตัวอย่างก่อนพิมพ์ว่าตารางไม่ขาดกลางหน้า
8. เทียบจำนวนสัญญาณกับก่อนแก้ ถ้าต่างกันมากผิดปกติให้หาสาเหตุก่อน commit

ตรวจกับไฟล์ที่มีเคสหลากหลายอย่างน้อยหนึ่งไฟล์ อย่าตรวจเฉพาะไฟล์ที่ข้อมูลสะอาด เพราะจะไม่เห็นว่าเงื่อนไขทำงานหรือไม่ ถ้าต้องทดสอบฟีเจอร์ที่ข้อมูลจริงยังไม่มี เช่น หลายโรงเรียนหรือคอลัมน์เวลา ให้สร้างไฟล์สังเคราะห์ขึ้นมาทดสอบ

---

## ข้อควรระวัง

**ห้าม commit ไฟล์ข้อมูลผู้ตอบ** ไฟล์ส่งออกมีชื่อ–สกุลจริงและวันเกิดของนักเรียน `.gitignore` บล็อกไว้แล้วแต่การลากไฟล์วางบนหน้าเว็บ GitHub จะไม่ผ่าน `.gitignore` ตรวจ `git status` ทุกครั้งก่อน commit

**ห้ามใช้ localStorage หรือ sessionStorage** ระบบตั้งใจไม่เก็บอะไรค้างในเบราว์เซอร์ เพื่อให้ปิดหน้าเว็บแล้วข้อมูลหายจริง

**ห้ามเพิ่มการเรียก network** ระบบต้องทำงานได้แบบออฟไลน์ทั้งหมด ไลบรารีทุกตัวฝังอยู่ในไฟล์แล้ว ยกเว้นฟอนต์จาก Google Fonts ซึ่งถ้าโหลดไม่ได้จะถอยไปใช้ฟอนต์ระบบเอง

**อย่าแก้ค่าเกณฑ์โดยไม่บันทึกเหตุผล** เกณฑ์ทุกตัวมีที่มา ถ้าปรับเพราะข้อมูลจริงจากพื้นที่บอกอย่างนั้น ให้ระบุใน `CHANGELOG.md` ว่าปรับจากอะไรเป็นอะไรและเพราะอะไร มิฉะนั้นรอบเก็บถัดไปจะไม่มีใครรู้ว่าเกณฑ์ที่ใช้อยู่มาจากไหน

---

## การอัปเดตเอกสารข้อกำหนด

โครงการมีเอกสารข้อกำหนดเงื่อนไขการตรวจคุณภาพข้อมูลฉบับ Word ซึ่ง **ไม่ได้เก็บไว้ในรีโพนี้** เพราะมีภาคผนวกที่อ้างถึงโรงเรียนนำร่องและผลการตรวจของโรงเรียนนั้น จึงเผยแพร่สาธารณะไม่ได้ ให้ขอไฟล์จากผู้ดูแลโครงการ

เอกสารฉบับนั้นสร้างจากทะเบียนสัญญาณในโค้ดโดยตรง จึงต้องสร้างใหม่ทุกครั้งที่แก้ `FLAGS` `BATCH_FLAGS` `PA_ITEMS` หรือ `MISC` มิฉะนั้นเอกสารกับระบบจะไม่ตรงกันภายในไม่กี่รอบการแก้ไข เมื่อแก้สิ่งเหล่านี้แล้วให้บันทึกไว้ใน `CHANGELOG.md` ด้วยเสมอ เพราะ changelog คือจุดเชื่อมเดียวระหว่างโค้ดในรีโพกับเอกสารที่อยู่นอกรีโพ
